#ifndef PROMOTION_INFO_H
#define PROMOTION_INFO_H

#include "point.h"

class ChessPiece;
class PieceSprite;

class PromotionInfo {
public:
  Point to;
  PieceSprite* piece;
  PieceSprite* promoted;
  
  PromotionInfo(const Point& to, PieceSprite* piece, PieceSprite* promoted)
  : to(to), piece(piece), promoted(promoted) {}
};

#endif // PROMOTION_INFO_H
